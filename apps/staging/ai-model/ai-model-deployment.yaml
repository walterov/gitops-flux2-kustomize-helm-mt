apiVersion: kaito.sh/v1alpha1
kind: Workspace
metadata:
  name: workspace-phi-2
  namespace: ai-workspace
resource:
  instanceType: "Standard_NC16_A2"
  labelSelector:
    matchLabels:
      app: llm-inference
inference:
  preset:
    name: "phi-2"
